# 3D Interstellar Map — Task Log 2026-04-19

## Version
**v0.1.3** (current deployed)

---

## ✅ Completed Today

### FRD-045 M1-M2: Star Generation Mode (CORE)
- [x] HYG / Generate mode toggle in Controls panel
- [x] Blank canvas generation mode
- [x] 5D6 class/grade tables (mirrors MWG exactly)
- [x] Pass-based generation algorithm
- [x] d66 → 360° spherical coordinate conversion
- [x] Origin star + child stars with correct distances
- [x] Greek alphabet naming (Alpha, Beta, Gamma...)
- [x] Generation stats stored per star (rolls, pass, parent, distance)

### Bug Fixes
- [x] **HYG mode switch bug**: `appMode` was captured by value in `ui.ts`, preventing round-trip HYG→Generate→HYG
  - Fix: removed guard condition, always call `onModeChange()`
- [x] **Sphere-corrected exponential growth**: star count now follows `distanceMultiplier³ × density` per pass
  - Volume ∝ r³ in 3D space; each shell has ~8× volume of previous when distance doubles
  - Density slider (0.1–2.0) acts as modifier on natural sphere density

### Infrastructure
- [x] `src/dice.ts` — dice rolling utilities (d66, D3, D6, ND6)
- [x] `src/generator.ts` — star generation engine with editable tables support
- [x] `src/types.ts` — `GeneratedStar`, `GenerationParameters`, `GenerationTables`
- [x] Build clean, zero TypeScript errors
- [x] Committed & pushed to `main`

---

## 🔄 In Progress / Partially Done

### FRD-045 M3: Tables + Context Panel
- [ ] **Editable class/grade tables** — UI structure exists (`#tables-panel`, `.gen-table`), but not wired to generation
  - `renderTables()` defined in `main.ts` but not called on init
  - `cycleClass()` / `cycleGrade()` defined but not tested
  - Need to: call `renderTables()` when entering Generate mode, regenerate on table change
- [ ] **Generation stats in context panel** — `showStarContextPanel()` added, shows `_generation` block
  - Need to: add "Load MWG JSON" button to context panel for GeneratedStars

### FRD-045 M4: Save/Export
- [ ] **`.mneme-map` export format** — type `MnemeMapExport` defined, no export function
- [ ] **`.mneme-map` import** — no parser
- [ ] **Attach MWG JSON** — no file picker in context panel

---

## 📋 Queued (Next Sessions)

### Priority 1: Complete FRD-045
1. Wire editable tables → regenerate on cell click
2. `.mneme-map` save/load/export/import
3. Context panel "Load MWG JSON" button
4. Accept and close FRD-045

### Priority 2: FRD-047 (MWG Batch Management)
1. `StarSystemBatch` data model in MWG
2. IndexedDB migration for legacy systems
3. Systems View with batch selector
4. Import `.mneme-map` as batch
5. Export batch to `.mneme-map`

### Priority 3: FRD-046 (2D Map Star Page Save)
1. Self-contained HTML export
2. Shared storage with 3D map
3. MWG Editor tab in 2D map

---

## 🔧 Known Issues / QA Queue

| ID | Issue | Status |
|---|---|---|
| QA-001 | `renderTables()` not called on init — tables invisible until first generate | 🔴 Open |
| QA-002 | `mapName` variable declared but unused — need for `.mneme-map` save | 🟡 Expected |
| QA-003 | `MnemeMapExport` type imported but unused — waiting for M4 | 🟡 Expected |
| QA-004 | Context panel missing "Load MWG JSON" file picker for GeneratedStars | 🟡 Expected |
| QA-005 | `populateContextPanel` still imported from `ui.ts` but unused (dead code) | 🟢 Low priority |

---

## 🎯 Next Session — Recommended

**"continue M3-M4"** → wire tables, add save/export, close FRD-045

Files to touch:
- `src/main.ts` — call `renderTables()`, add `.mneme-map` save/load
- `src/ui.ts` — add "Load MWG JSON" to context panel HTML
- `src/generator.ts` — (minor) ensure tables are passed through correctly

---

## 📁 Related FRDs

- `FRD-045-star-generation-mode.md` — this project
- `FRD-046-star-page-save.md` — 2D map project
- `FRD-047-batch-management.md` — MWG project
- `FRD-MASTER-IMPLEMENTATION-QUEUE.md` — cross-project roadmap
